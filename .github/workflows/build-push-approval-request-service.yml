name: Build and Push Approval Request Service Image

on:
  push:
    branches:
      - main
    paths:
      - 'approval-request-service/**'
      - '.github/workflows/build-push-approval-request-service.yml'

jobs:
  build-and-push-approval-request-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Github Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_SECRET }}

      - name: Build and push Approval Request Service Image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: approval-request-service/Dockerfile
          push: true
          tags: ghcr.io/ilcm96/dku-ce-erp-microservices/approval-request-service:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

